name: MSI_tank
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy>=1.24.0
  - scipy>=1.10.0
  - numba>=0.56.0
  - matplotlib>=3.6.0   # Opcjonalne: Wykresy wydajności
  - pillow>=9.0.0       # Opcjonalne: Przetwarzanie obrazów/tekstur
  - psutil>=5.9.0       # Opcjonalne: Monitorowanie CPU/RAM
  - tqdm>=4.64.0        # Opcjonalne: Paski postępu w konsoli
  - pip
  - pip:
      - pygame>=2.5.0
      - arcade>=2.6.0
      - dataclasses-json>=0.5.0
      - pydantic>=1.10.0
      - colorlog>=6.7.0
      - httpx>=0.24.0
      - pytest>=7.0.0
      - pytest-cov>=4.0.0
      - black>=23.0.0
      - flake8>=6.0.0
      - mypy>=1.0.0
      - jupyterlab>=3.6.0   # Opcjonalne: Analiza w notatnikach
      - ipython>=8.0.0      # Opcjonalne: Lepsza konsola
      - fastapi>=0.100.0    # Opcjonalne: 'random_agent.py' 
      - uvicorn>=0.23.0     # Opcjonalne: 'random_agent.py'